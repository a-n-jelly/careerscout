#!/bin/bash
# feed-agent/config.sh — user-specific paths
# Copy this file to config.sh (gitignored) and fill in your paths.
# Generated by /feed-setup; edit manually if needed.
#
# Find your claude path: which claude
# Find your python path: which python3  (or use .venv if you set one up)

export CLAUDE_BIN="/usr/local/bin/claude"    # replace with: which claude
# export PYTHON_BIN="/path/to/python3"       # optional — defaults to .venv
